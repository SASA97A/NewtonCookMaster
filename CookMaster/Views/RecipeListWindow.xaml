<Window x:Class="CookMaster.Views.RecipeListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views"
        mc:Ignorable="d"
        Title="Recipe List" Height="650" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Background="LightGray">
            <TextBlock x:Name="txtWelcome" Text="Hello, User" VerticalAlignment="Center"/>
            <Button Content="My Profile" Click="Profile_Click" Margin="10,0"/>
            <Button Content="Logout" Click="Logout_Click" HorizontalAlignment="Right"/>
        </StackPanel>

        <Button Grid.Row="0" Content="+ Add Recipe" Click="AddRecipe_Click" HorizontalAlignment="Right" Margin="0,0,100,0"/>

        <ListBox x:Name="lstRecipes" Grid.Row="1" Margin="10" SelectionMode="Single"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Details" Click="Details_Click" Width="100"/>
            <Button Content="Remove" Click="Remove_Click" Width="100" Margin="20,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
