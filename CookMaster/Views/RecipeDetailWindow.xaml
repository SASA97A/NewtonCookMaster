<Window x:Class="CookMaster.Views.RecipeDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views"
        mc:Ignorable="d"
        Title="Recipe Detail" Height="650" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Recipe Title" FontWeight="Bold"/>
            <TextBox x:Name="txtTitle" Margin="0,5,0,10" IsReadOnly="True"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                    <TextBlock Text="Category" FontWeight="Bold"/>
                    <TextBox x:Name="txtCategory" Margin="0,5,0,10" IsReadOnly="True" Visibility="Visible"/>

                    <ComboBox x:Name="cmbCategoryEdit" Margin="0,5,0,10" Visibility="Collapsed">
                        <ComboBoxItem Content="Italian"/>
                        <ComboBoxItem Content="Asian"/>
                        <ComboBoxItem Content="Mexican"/>
                        <ComboBoxItem Content="American"/>
                        <ComboBoxItem Content="French"/>
                        <ComboBoxItem Content="Indian"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                    <TextBlock Text="Date Added" FontWeight="Bold"/>
                    <TextBox x:Name="txtDate" Margin="0,5,0,10" IsReadOnly="True" Background="#F0F0F0"/>
                </StackPanel>
            </Grid>
        </StackPanel>

        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <TextBlock Text="Ingredients" FontWeight="Bold"/>
                <TextBox x:Name="txtIngredients" Height="80" TextWrapping="Wrap" AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto" Margin="0,5,0,10" IsReadOnly="True"/>

                <TextBlock Text="Instructions" FontWeight="Bold"/>
                <TextBox x:Name="txtInstructions" Height="150" TextWrapping="Wrap" AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto" Margin="0,5,0,10" IsReadOnly="True"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="btnEdit" Content="Edit" Click="Edit_Click" Width="80" Margin="0,0,10,0"/>
            <Button x:Name="btnSave" Content="Save" Click="Save_Click" Width="80" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>
