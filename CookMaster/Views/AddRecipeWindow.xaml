<Window x:Class="CookMaster.Views.AddRecipeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CookMaster.Views"
        mc:Ignorable="d"
        Title="Add New Recipe" Height="650" Width="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Add New Recipe" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="Title *"/>
            <TextBox x:Name="txtTitle" Margin="0,5,0,10"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,5,0">
                    <TextBlock Text="Category *"/>
                    <ComboBox x:Name="cmbCategory" Margin="0,5,0,10">
                        <ComboBoxItem Content="Italian"/>
                        <ComboBoxItem Content="Asian"/>
                        <ComboBoxItem Content="Mexican"/>
                        <ComboBoxItem Content="American"/>
                        <ComboBoxItem Content="French"/>
                        <ComboBoxItem Content="Indian"/>
                    </ComboBox>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="5,0,0,0">
                    <TextBlock Text="Date Added"/>
                    <DatePicker x:Name="datePicker" Margin="0,5,0,10" Focusable="False"/>
                </StackPanel>
            </Grid>

            <TextBlock Text="Ingredients *"/>
            <TextBox x:Name="txtIngredients" Height="80" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,5,0,10"/>

            <TextBlock Text="Instructions *"/>
            <TextBox x:Name="txtInstructions" Height="120" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,5,0,20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" IsCancel="True" Width="80" Margin="0,0,10,0"/>
                <Button Content="Save Recipe" Click="Save_Click" Width="100" FontWeight="Bold"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
